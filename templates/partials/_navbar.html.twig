<nav>
    <a href="{{ path('portfolio_home') }}" class="logo">
        <img src="{{ asset('images/logo.png') }}" alt="Logo">
    </a>
    <div class="mobile-lines">
        <span class="line"></span>
        <span class="line"></span>
        <span class="line"></span>
    </div>
    <ul>
        <li><a href="{{ path('portfolio_home') }}">{{ "navbar.menu.home" | trans({}, "navbar") }}</a></li>
        <li><a href="{{ path('portfolio_about') }}">{{ "navbar.menu.about" | trans({}, "navbar") }}</a></li>
        <li><a href="{{ path('portfolio_projects') }}">{{ "navbar.menu.projects" | trans({}, "navbar") }}</a></li>
        <li><a href="{{ path('portfolio_contact') }}">{{ "navbar.menu.contact" | trans({}, "navbar") }}</a></li>
    </ul>
</nav>